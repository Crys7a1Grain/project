<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= recipe.name %></title>
    <link rel="stylesheet" href="/normalize.css" />
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <header class="header">
      <div class="container">
        <div class="header-content">
          <a class="title header-title" href="/"
            >Поиск рецептов по ингредиентам</a
          >
          <a class="title header-title" href="/recipes">Рецепты</a>
        </div>
      </div>
    </header>

    <div class="main">
      <div class="container">
        <div class="main-content">
          <img
            class="recipe-image"
            src="data:image/jpg;base64,<%= recipe.image %>"
            alt="<%= recipe.name %>"
          />
          <div class="content-box">
            <p class="item-name"><%= recipe.name %></p>
            <p class="item-description recipe-margin">
              <%= recipe.description %>
            </p>

            <p class="ingredients-title">Ингредиенты на 3 порции:</p>
            <ul class="ingredient-list">
              <% recipe.ingredients.forEach((ingredient) => { %>
              <li class="ingredient-item">
                <%= ingredient.name %> - <%= ingredient.volume %>
              </li>
              <% }); %>
            </ul>

            <p class="instructions-title">Инструкция приготовления:</p>
            <% recipe.instructions.split('\\n').forEach((instruction) => { %> <%
            const cleanInstruction = instruction.trim(); %> <% if
            (cleanInstruction) { %>
            <p class="item-instruction"><%= cleanInstruction %></p>
            <% } %> <% }); %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
