<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= recipe.name %></title>
    <link rel="stylesheet" href="/normalize.css" />
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <header class="header">
      <div class="container" style="align-items: center; gap: 200px">
        <h1 class="title">Поиск рецептов по ингредиентам</h1>
        <a class="title" href="/recipes">Рецепты</a>
      </div>
    </header>

    <div class="main">
      <div class="container">
        <div>
          <img
            class="item-image"
            src="data:image/jpg;base64,<%= recipe.image %>"
            alt="<%= recipe.name %>"
          />
          <div class="item-box">
            <p class="item-name"><%= recipe.name %></p>
            <p class="item-description"><%= recipe.description %></p>
            <% recipe.instructions.split('\\n').forEach((instruction) => { %>
              <% const cleanInstruction = instruction.trim(); %>
              <% if (cleanInstruction) { %>
                <p class="item-instruction"><%= cleanInstruction %></p>
              <% } %>
            <% }); %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
